return {
  {
    "nvim-tree/nvim-web-devicons",
    lazy = true,  -- Set to true to load the plugin lazily
    opts = {
      -- Optional: Customize default icon and color
      default = true,
      color_icons = true,
    },
  },
}
